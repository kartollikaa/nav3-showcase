<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@id/dest_auth"
    app:startDestination="@id/authFragment">

    <!-- One fragment now. The phone/sms/name steps live on a Nav3 back stack INSIDE it. Other modules
         reach the flow by the shared @id/dest_auth (this graph's id, declared in :core:designsystem). -->
    <fragment
        android:id="@+id/authFragment"
        android:name="io.dodo.nav3.feature.auth.AuthFragment"
        android:label="Auth" />
</navigation>
